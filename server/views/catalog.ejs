<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="theme-color" content="#000" />
  <title><%= pageTitle %></title> 
  <link rel="icon" href="/image/logo.webp" type="image/png">
  <link rel="stylesheet" href="/stylescatalog.css" />
</head>
<body>

  <div class="contact-overlay" id="contactOverlay" onclick="closeContact()"></div>
  <div class="horizontal-contact" id="horizontalContact" onclick="openContact()">Зв'язок</div>
  <div class="contact-bar" id="contactBar">
    <div class="contact-content">
      <div class="contact-header">
        <span>Зв'язок</span>
        <span class="close-btn" onclick="closeContact()">✖</span>
      </div>
      <label>Email:</label>
      <input type="email" id="email" value="zippoziga32@gmail.com" readonly />
      <label>Повідомлення:</label>
      <textarea id="message" placeholder="Введіть ваше повідомлення..."></textarea>
      <button onclick="sendMail()">Відправити</button>      
    </div>
  </div>

  <header>
    <div class="left-section">
      <div class="logo">Форма3D</div>
      
      <div id="favoritesIcon" title="Улюблені товари" style="cursor:pointer; position: relative; color: rgb(255, 255, 255); font-size: 34px; margin-left: 20px; padding-bottom: 5px;">
        ★
        <span id="favoritesCount" style="position: absolute; top: -3px; right: -8px; background: red; color: white; border-radius: 50%; padding: 1px 6px; font-size: 14px; display: none; user-select: none;">0</span>
      </div>
    </div>
    <a href="/" class="back-link">← На головну</a>
  </header>

  <main>
    <div class="filter-panel">
      <div class="search-box">
        <input type="text" id="searchInput" placeholder="Пошук за назвою..." onkeyup="filterProducts()">
      </div>
      
      <div class="filters">
        <select id="categoryFilter" onchange="filterProducts()">
          <option value="all">Всі категорії</option>
          <% if (types && types.length > 0) { %>
            <% types.forEach(type => { %>
              <option value="<%= type.id %>"><%= type.name %></option>
            <% }); %>
          <% } %>
        </select>

        <select id="priceSort" onchange="sortProducts()">
          <option value="default">Сортувати за ціною</option>
          <option value="low-to-high">Від дешевих до дорогих</option>
          <option value="high-to-low">Від дорогих до дешевих</option>
        </select>
      </div>
    </div>

    <section id="store" class="section">
      <div class="product-grid" id="productGrid">
        <% if (products && products.length > 0) { %>
            <% products.forEach(function(item) { %>
                <div class="product-card" 
                     data-id="<%= item.id %>" 
                     data-name="<%= item.name.toLowerCase() %>" 
                     data-price="<%= item.price %>" 
                     data-category="<%= item.typeId %>">
                    
                    <div style="display: block;">
                        <img src="/<%= item.img %>" alt="<%= item.name %>" onerror="this.src='/image/logo.webp'" />
                    </div>
                    
                    <h3><%= item.name %></h3>
                    <p class="price-value"><%= item.price %> ГРН</p>
                    
                    <div class="actions">
                        <a href="/product/<%= item.id %>" class="buy-btn">Купити</a> 
                        <span class="star-icon" title="Додати в улюблені">☆</span>
                    </div>
                </div>
            <% }); %>
        <% } else { %>
            <p id="noProducts" style="text-align: center; color: white; grid-column: 1 / -1;">Товарів поки немає :(</p>
        <% } %>
      </div>
    </section>
  </main>

  <footer>
    <div id="footer" class="footer-content">
      <p>@ Пошта: <a href="mailto:zippoziga32@gmail.com">zippoziga32@gmail.com</a></p>
      <p>✆ Телефон: <a href="tel:0677772819">0677772819</a></p>
      <p>✈ Telegram: <a href="https://t.me/zippoziga">@zippoziga</a></p>
      <p>✎ Маєш ідеї? <a href="https://forms.gle/ZAtcrnYGYUspi13p6">Залишити відгук</a></p>
      <p>✓ Умови <a href="/privacy">Політика конфіденційності</a></p>
    </div>
  </footer>

  <script src="/script.js"></script>
</body>
</html>